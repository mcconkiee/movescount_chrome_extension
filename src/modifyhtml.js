import axios from 'axios';
import $ from 'jquery';
window.onload = function() {
  // Move.findAll((error, results) => {
  //   console.log(error, results, 'findAll Moves');
  // });
};

// $('#summarySelection').append('<div>Download </div>');
